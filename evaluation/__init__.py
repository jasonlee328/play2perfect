"""evaluation: the four Play2Perfect assembly tasks.

- ``evaluation.objects``  — the ``NAME_TO_OBJECT`` registry (insertion objects).
- ``evaluation.problems`` — the ``PROBLEM_REGISTRY`` of the four released tasks.
- ``evaluation.eval_isaacsim`` — interactive (viser) evaluation.
"""
